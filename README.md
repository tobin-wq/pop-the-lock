# Pop the Lock :unlock:
A terminal version of the classic Pop the Lock game

Credit to $\color{red}{\textsf{Bay Tek Entertainment}}$ for creating Pop the Lock

Make sure to resize the terminal window to fit the ascii (<ins>101x66 is recommended<ins>)

For Mac or Linux paste into Terminal or Bash:
```
curl -sSL https://raw.githubusercontent.com/tobin-wq/pop-the-lock/main/install.sh | bash
```
For Windows cmd.exe paste into Command Prompt:
```
powershell -c "irm https://raw.githubusercontent.com/tobin-wq/pop-the-lock/main/install.ps1 | iex"
```
For PowerShell or Windows Terminal paste into a PowerShell window or Windows Terminal:
```
irm https://raw.githubusercontent.com/tobin-wq/pop-the-lock/main/install.ps1 | iex
```
Command Prompt, PowerShell, and Windows Terminal will all be able to run the command, but Windows Terminal will work the best.

---
**About**

Pop the Lock is a fast-paced timing game where the goal is to stop the rotating indicator inside the target zone before it spins past. Land the hit and the lock advances to the next level. Each level is harder than the last. This is a terminal-based recreation of the original game by Bay Tek Entertainment.
Press SPACE to lock, P to pause, and ESC to escape the pop the lock window.
 
**Author**

Made by Tobin Maymin who is an 11-year-old developer who uses AI to help make things. He's currently building his portfolio.
